<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body class="container mt-5">

    <div class="card">
        <div class="card-header">
            Fiche Client #<%= client.idClient %>
        </div>
        <div class="card-body">
            <h5 class="card-title">
                <%= client.prenom %>
                    <%= client.nom %>
            </h5>
            <p class="card-text">
                <strong>Email :</strong>
                <%= client.email %><br>
                    <strong>Téléphone :</strong>
                    <%= client.telephone %>
            </p>

            <a href="/clients/update/<%= client.idClient %>" class="btn btn-warning">Modifier</a>

            <form action="/clients/delete/<%= client.idClient %>" method="POST" style="display:inline;"
                onsubmit="return confirm('Confirmer la suppression ?');">
                <button type="submit" class="btn btn-danger">Supprimer</button>
            </form>

            <a href="/clients" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>

</body>

</html>