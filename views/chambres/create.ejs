<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        /* Un peu de style pour aérer le formulaire */
        form { max-width: 400px; margin-top: 20px; }
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; }
        input, select { width: 100%; padding: 8px; box-sizing: border-box; }
        button { padding: 10px 15px; background-color: #27ae60; color: white; border: none; cursor: pointer; font-size: 16px; }
        button:hover { background-color: #219150; }
        .cancel-link { margin-left: 10px; color: #7f8c8d; text-decoration: none; }
    </style>
</head>
<body>

    <h1>Ajouter une nouvelle chambre</h1>
    
    <% if (typeof error !== 'undefined' && error) { %>
        <div style="background-color: #e74c3c; color: white; padding: 10px; margin-bottom: 20px; border-radius: 5px;">
            ⚠️ <%= error %>
        </div>
    <% } %>

    <form action="/chambres/create" method="POST">
        
        <div class="form-group">
            <label for="numero">Numéro de la chambre :</label>
            <input type="number" id="numero" name="numero" required placeholder="Ex: 101">
        </div>

        <div class="form-group">
            <label for="capacite">Capacité (personnes) :</label>
            <input type="number" id="capacite" name="capacite" required min="1" placeholder="Ex: 2">
        </div>

        <div class="form-group">
            <label for="disponibilite">Statut actuel :</label>
            <select id="disponibilite" name="disponibilite">
                <option value="1" selected>Libre</option>
                <option value="0">Occupée</option>
            </select>
        </div>

        <button type="submit">Créer la chambre</button>
        <a href="/chambres" class="cancel-link">Annuler</a>

    </form>

</body>
</html>